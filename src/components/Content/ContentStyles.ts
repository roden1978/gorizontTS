import {makeStyles} from '@material-ui/core/styles'

export const useStyles = makeStyles(theme => ({
    content: {
        flexGrow: 1,
        marginTop: 270,
    },
    mobileContent: {
        flexGrow: 1,
    }
}))